import initialState from './initialState';

// 执行对应的action
export default function homeActions(state = initialState, action) {
    switch (action.type) {
        default:
            return state
    }
};